# This repository is a curated Markdown awesome-list and needs no environment variables
# to build, lint, or test. This file exists to document that fact for contributors and
# AI agents. Add any future variables here (without real values) as `NAME=` entries.
